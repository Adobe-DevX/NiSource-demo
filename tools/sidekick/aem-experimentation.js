/**
 * Optional bridge for AEM Sidekick + experimentation.
 * Replace or extend this module if your project ships a custom Sidekick integration.
 */
export default function noopSidekickExperimentation() {}
