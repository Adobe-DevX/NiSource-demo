# Grid Layout

Grid-style container block that supports authorable rows/columns and nested blocks per cell.

## Authoring fields

- `columns`
- `rows`

## Notes

- Uses the same authoring model as `columns` (UE columns component).
- Nested key-value content is hydrated into matching block models.
